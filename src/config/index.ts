export { Routes,
         PropertyConfigurer,
         Config } from './config';

export { PassportConfig, 
         PassportConfigurer } from './auth-config';

export { AppConfig, AppConfigurer } from './app-config';
